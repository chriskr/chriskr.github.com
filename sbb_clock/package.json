{
  "name": "sbb_clock",
  "version": "1.0.0",
  "scripts": {
    "build": "babel src --presets es2015,stage-0 -d es2015 && npm run copy",
    "copy":  "copyfiles --up 1 src/*.html src/*.css src/*.svg src/*.jpg src/*.png es2015/"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-stage-0": "^6.16.0",
    "copyfiles": "latest"
  }
}
